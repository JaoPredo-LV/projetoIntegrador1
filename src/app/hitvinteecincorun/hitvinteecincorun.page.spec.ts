import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteecincorunPage } from './hitvinteecincorun.page';

describe('HitvinteecincorunPage', () => {
  let component: HitvinteecincorunPage;
  let fixture: ComponentFixture<HitvinteecincorunPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteecincorunPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
