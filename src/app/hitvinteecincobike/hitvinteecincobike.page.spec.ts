import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteecincobikePage } from './hitvinteecincobike.page';

describe('HitvinteecincobikePage', () => {
  let component: HitvinteecincobikePage;
  let fixture: ComponentFixture<HitvinteecincobikePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteecincobikePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
