import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteecincoelipticoPage } from './hitvinteecincoeliptico.page';

describe('HitvinteecincoelipticoPage', () => {
  let component: HitvinteecincoelipticoPage;
  let fixture: ComponentFixture<HitvinteecincoelipticoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteecincoelipticoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
