import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrcvientecincoeliPage } from './trcvientecincoeli.page';

describe('TrcvientecincoeliPage', () => {
  let component: TrcvientecincoeliPage;
  let fixture: ComponentFixture<TrcvientecincoeliPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrcvientecincoeliPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
