import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteesteiraPage } from './hitvinteesteira.page';

describe('HitvinteesteiraPage', () => {
  let component: HitvinteesteiraPage;
  let fixture: ComponentFixture<HitvinteesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
