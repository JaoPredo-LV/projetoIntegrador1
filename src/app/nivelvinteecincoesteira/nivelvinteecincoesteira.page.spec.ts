import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NivelvinteecincoesteiraPage } from './nivelvinteecincoesteira.page';

describe('NivelvinteecincoesteiraPage', () => {
  let component: NivelvinteecincoesteiraPage;
  let fixture: ComponentFixture<NivelvinteecincoesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NivelvinteecincoesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
