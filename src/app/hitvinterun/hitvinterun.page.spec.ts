import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinterunPage } from './hitvinterun.page';

describe('HitvinterunPage', () => {
  let component: HitvinterunPage;
  let fixture: ComponentFixture<HitvinterunPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinterunPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
