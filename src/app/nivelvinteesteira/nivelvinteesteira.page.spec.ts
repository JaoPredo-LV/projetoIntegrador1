import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NivelvinteesteiraPage } from './nivelvinteesteira.page';

describe('NivelvinteesteiraPage', () => {
  let component: NivelvinteesteiraPage;
  let fixture: ComponentFixture<NivelvinteesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NivelvinteesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
