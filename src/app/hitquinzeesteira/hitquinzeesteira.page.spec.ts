import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitquinzeesteiraPage } from './hitquinzeesteira.page';

describe('HitquinzeesteiraPage', () => {
  let component: HitquinzeesteiraPage;
  let fixture: ComponentFixture<HitquinzeesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitquinzeesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
