import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteecincoesteiraPage } from './hitvinteecincoesteira.page';

describe('HitvinteecincoesteiraPage', () => {
  let component: HitvinteecincoesteiraPage;
  let fixture: ComponentFixture<HitvinteecincoesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteecincoesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
