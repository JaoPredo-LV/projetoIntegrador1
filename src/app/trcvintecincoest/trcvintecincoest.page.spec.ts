import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrcvintecincoestPage } from './trcvintecincoest.page';

describe('TrcvintecincoestPage', () => {
  let component: TrcvintecincoestPage;
  let fixture: ComponentFixture<TrcvintecincoestPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrcvintecincoestPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
