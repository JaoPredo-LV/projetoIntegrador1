import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HidezrunPage } from './hitdezrun.page';

describe('HidezrunPage', () => {
  let component: HidezrunPage;
  let fixture: ComponentFixture<HidezrunPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HidezrunPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
