import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NiveleliptcoPage } from './niveleliptco.page';

describe('NiveleliptcoPage', () => {
  let component: NiveleliptcoPage;
  let fixture: ComponentFixture<NiveleliptcoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NiveleliptcoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
