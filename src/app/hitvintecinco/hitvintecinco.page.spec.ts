import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvintecincoPage } from './hitvintecinco.page';

describe('HitvintecincoPage', () => {
  let component: HitvintecincoPage;
  let fixture: ComponentFixture<HitvintecincoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvintecincoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
