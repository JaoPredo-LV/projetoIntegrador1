import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitquinzeelipticoPage } from './hitquinzeeliptico.page';

describe('HitquinzeelipticoPage', () => {
  let component: HitquinzeelipticoPage;
  let fixture: ComponentFixture<HitquinzeelipticoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitquinzeelipticoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
