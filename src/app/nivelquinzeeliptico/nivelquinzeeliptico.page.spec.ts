import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NivelquinzeelipticoPage } from './nivelquinzeeliptico.page';

describe('NivelquinzeelipticoPage', () => {
  let component: NivelquinzeelipticoPage;
  let fixture: ComponentFixture<NivelquinzeelipticoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NivelquinzeelipticoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
