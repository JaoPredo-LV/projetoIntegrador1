import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NivelquinzeesteiraPage } from './nivelquinzeesteira.page';

describe('NivelquinzeesteiraPage', () => {
  let component: NivelquinzeesteiraPage;
  let fixture: ComponentFixture<NivelquinzeesteiraPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NivelquinzeesteiraPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
