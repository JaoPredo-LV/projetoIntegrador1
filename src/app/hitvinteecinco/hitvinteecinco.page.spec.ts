import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HitvinteecincoPage } from './hitvinteecinco.page';

describe('HitvinteecincoPage', () => {
  let component: HitvinteecincoPage;
  let fixture: ComponentFixture<HitvinteecincoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HitvinteecincoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
