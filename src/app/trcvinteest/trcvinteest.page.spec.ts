import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrcvinteestPage } from './trcvinteest.page';

describe('TrcvinteestPage', () => {
  let component: TrcvinteestPage;
  let fixture: ComponentFixture<TrcvinteestPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrcvinteestPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
