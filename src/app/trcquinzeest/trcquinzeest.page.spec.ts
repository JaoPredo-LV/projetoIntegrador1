import { ComponentFixture, TestBed } from '@angular/core/testing';
import { TrcquinzeestPage } from './trcquinzeest.page';

describe('TrcquinzeestPage', () => {
  let component: TrcquinzeestPage;
  let fixture: ComponentFixture<TrcquinzeestPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(TrcquinzeestPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
