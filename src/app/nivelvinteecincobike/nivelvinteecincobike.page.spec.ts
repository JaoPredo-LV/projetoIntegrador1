import { ComponentFixture, TestBed } from '@angular/core/testing';
import { NivelvinteecincobikePage } from './nivelvinteecincobike.page';

describe('NivelvinteecincobikePage', () => {
  let component: NivelvinteecincobikePage;
  let fixture: ComponentFixture<NivelvinteecincobikePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(NivelvinteecincobikePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
